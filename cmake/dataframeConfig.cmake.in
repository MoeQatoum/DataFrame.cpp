@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@_Targets.cmake")

check_required_components("@PROJECT_NAME@")

include(CMakeFindDependencyMacro)

message(STATUS "@PROJECT_NAME@ found.")

set(DF_FOUND TRUE)
set(dataframe_FOUND TRUE)

set(DF_INCLUDE_DIR "@CMAKE_INSTALL_FULL_INCLUDEDIR@")
set(DF_LIB_DIR "@CMAKE_INSTALL_FULL_LIBDIR@")
